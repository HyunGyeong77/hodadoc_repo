export interface Props {
    [key: string]: {
        image: string,
        title: string,
        type: string[],
        rating: string,
        addr: string,
        time: string[][],
        site: string
    }
}